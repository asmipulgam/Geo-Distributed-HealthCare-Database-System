code for main backend / central server which queries with MongoDB to fetch concerned node server and forward data accordingly.
Perform health checks, decide primary and secondary, spin up new instance of DB if required.
